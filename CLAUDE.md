# 給 Claude 的專案須知

這個檔案會被 Claude Code 自動讀取。**動手前先讀完，不要憑印象改。**
操作細節（新增文章、改診所資料、部署流程）寫在 `README.md`，這裡只放「一定要遵守的規則」。

---

## 這是什麼

君禾診所（新竹縣湖口鄉）的官方網站。**純靜態，沒有資料庫、沒有後台、沒有前端框架、零外部套件。**

`tools/build.mjs` 是自製產生器，把 `src/pages/*.mjs`（固定頁）與
`content/articles/*.md`（文章）輸出成 `dist/`。

```
site.config.mjs        全站設定（名稱、電話、地址、看診時間、頁籤、SEO）——單一來源
src/layout.mjs         版型與結構化資料
src/pages/*.mjs        各頁面
content/articles/*.md  衛教文章
assets/                CSS、圖片、JS
tools/build.mjs        建置腳本
```

**改完一定要跑 `npm run build`**，否則 `dist/` 還是舊的。
`dist/` 不進版控；push 到 `main` 之後由 Cloudflare 重建，**約 1～2 分鐘才會更新**。
剛 push 就去抓網頁會拿到舊版，多等一輪再確認，不要重推。

正式網址：<https://harmoniaclinic.net>（`www` 與 `*.workers.dev` 由 `worker.js` 301 轉過來）

---

## 一、醫療文案（最重要）

### 不可以出現「家醫科」

診所的**開業登記診療科別是「一般科」**。黃佳君醫師具**麻醉科**部定專科資格，**沒有家庭醫學科專科**。

- 全站禁止出現「家醫科」「家庭醫學科」「家醫照護」等字樣。
- 網站主人也**不要寫出「一般科」三個字**。
- 正確做法：**只描述實際看診範圍，完全不提科別**。
  例如「日常的大小病，以及各種疼痛問題」「從慢性病追蹤、感冒過敏等急性不適，到肩頸腰背、關節及神經相關疼痛」。

「疼痛科」在台灣**不是衛福部部定專科**，但**網站主人已決定**：文章作者頭銜一律寫
**「麻醉科．疼痛科專科醫師」**（`content/articles/*.md` 的 `authorRole` 欄位），三篇文章目前一致，
不要再改成別的寫法，也不用每次重新提醒。

這條只管作者頭銜。其他地方仍然**不要把「疼痛科」當診所的登記科別寫**——
診所登記的是一般科，這點沒有變。「疼痛診療」這種描述服務的寫法可以用。

實際看診範圍：慢性病追蹤、**感冒／呼吸道／腸胃道／皮膚／過敏**等急性症狀、
肌肉骨骼神經疼痛、再生注射、預防保健與疫苗施打、營養點滴。寫摘要句時不要漏掉急性症狀那一塊。

### 寫法原則

只寫「這是什麼、過程如何」，**不寫療效保證與成功率**，適不適合一律導回「由醫師門診評估後說明」，
自費項目要標明。

把絕對敘述改成有條件的敘述，這是全站一致的方向：

| 不要這樣寫 | 要這樣寫 |
|---|---|
| 輻射劑量比傳統低 | 實際輻射量依拍攝部位、姿勢、設備與參數而不同 |
| 不會疼痛 | 大部分情況下不會疼痛 |
| 位置更明確、減少傷害 | 有助於提升部分關節及軟組織注射的定位準確性 |

### 代擬的內容要標示出來

「診所實際上怎麼做」只有醫師知道。凡是 Claude 草擬的衛教內容，**交付時一定要明講哪些是代擬的**，
請黃醫師過目後才視為定稿。不要當成診所既有文案直接上線。

---

## 二、語言

**一律使用繁體中文**，對話與網站文字都不可以出現簡體字。

說明要白話，避免術語。提到 SEO、結構化資料、快取這類概念時，用生活化的比喻解釋清楚。
除了「改了什麼」，也要講「為什麼要這樣改、不這樣會怎樣」。

---

## 三、配色

視覺以診所 logo 為準。三條硬規則，**不要憑印象改**（細節見 `README.md` 的「Logo 與配色」）：

1. `--accent`（#d18f40 琥珀橘）**只做裝飾**（短線、hover 邊框、底線），**不能拿來寫字**。
   要暖色的字用 `--gold-600`。
2. `--accent` **不能貼在 `--brand` 上**，對比只有 1.11，會糊掉。
3. `--brand`（#9cab80 橄欖綠）底色上**只能放深色字**，不能放白字。

換首頁 banner 時，若新圖底色不是 #9cab80，**一定要同步改 `--brand`**，否則圖片兩側會有色差。

---

## 四、Git

**`git add` 之後一定要先看 `git status --short` 確認暫存區內容，確定無誤才 commit。**
不要把 `git add` 和 `git commit` 寫成連續兩行直接送出——`git add` 遇到不存在的檔案會整批放棄，
但後面的 commit 照樣執行，結果推出去的版本會是殘缺的，線上網站會壞掉。

repo 是**公開**的。外部產生的工作文件（稽核報告、電子報、問答草稿）**不要 commit**，
`.gitignore` 已有對應規則，新增這類檔案時一併加進去。

節奏是：**改完並驗證 → 報告 → 等他說「commit」「push」才做。** 不要自動提交。

---

## 五、圖片

放進 `assets/img/` 的任何檔案都會**原封不動發布到網路上**，處理完的原始檔不要留在那裡。

**原始檔一律放在 repo 外面的 `~/Pictures/君禾診所-原圖/`**（分成衛教插圖／診所空間／
醫師與證書／logo與識別四個子資料夾），檔名用「上線檔名_原圖_寬x高」。
處理任何一張新圖之前，先確認原始檔已經備份到那裡，再做上線版。細節見 `README.md`。

- **一定要清除 EXIF**。手機照片含 GPS 座標與機型，上線前必須移除。
- HEIC 要套用方向標記後再轉檔，否則會躺著。
- 版面上的照片統一 **3:2**，寬度 1200～1600 px 就夠。
- **換圖時把檔名也換掉**，否則瀏覽器會拿快取裡的舊圖，看不到新的。
- 分享縮圖（og:image）與結構化資料**不能用 SVG**，Google、LINE、Facebook 都不吃。

---

## 六、結構化資料

診所與醫師各有固定的實體編號，所有頁面指向同一組，不要另外描述一次：

- 診所 `https://harmoniaclinic.net/#clinic`
- 醫師 `https://harmoniaclinic.net/doctors/#physician`

`medicalSpecialty` 只用 schema.org 認可的代號。**`PainMedicine` 不是有效值**，不要寫。
目前用的是 `PrimaryCare`（指照護層級，不是家醫專科）、`Anesthesia`、`Musculoskeletal`。

`site.config.mjs` 的 `seo.sameAs` 是診所在其他平台的正式頁面，拿到新網址就加進去。
